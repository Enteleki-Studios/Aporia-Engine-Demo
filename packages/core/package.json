{
    "name": "@gengine/core",
    "version": "0.1.0",
    "main": "./src/index.ts",
    "scripts": {
        "start": "vite build --watch",
        "build": "vite build",
        "build:docs": "typedoc",
        "build:docs:watch": "typedoc --watch",
        "test": "jest",
        "typecheck": "tsc --noEmit",
        "lint": "eslint src",
        "lint:fix": "eslint src --fix"
    }
}
